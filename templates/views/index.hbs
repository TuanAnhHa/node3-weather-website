<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="utf-8">
    <title>Express</title>
    <link href="./css/styles.css" rel="stylesheet"/>
</head>
{{>header}}
    <div class="main-content">
        <h1>{{title}}</h1>
        <h2>by {{name}}</h2>
        <form>
            <input type="text" placeholder="What's the weather like in...?"/>
            <button>Search Now</button>
        </form>

        <p id="message-1"></p>
        <p id="message-2"></p>
    </div>

<script type="text/javascript" src="./js/app.js"></script>
{{>footer}}
